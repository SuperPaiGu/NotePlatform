services:
  web:
    build: .
    ports:
      - "8000:5000"
  
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: rootpass
      MYSQL_DATABASE: notedb
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: